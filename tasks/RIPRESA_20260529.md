# RIPRESA — sessione probe DAPI 2026-05-29

**Scritto**: 2026-05-29 ~01:51 CET (Claude CLI locale), prima dello spegnimento PC.
**Per**: la prossima sessione Claude CLI che apre stamattina/oggi.
**Briefing completo di riferimento**: `tasks/HANDOFF_PROBE_DAPI_20260528.md` (leggere quello per i 6 fatti DAPI, comandi V-1/V-2, vincoli). Questo file aggiunge SOLO lo stato operativo non ancora su disco.

---

## Stato repo
- `origin/main` @ `2dc457b` — allineato, working tree pulito.
- Commit aggiunto stanotte: `[PROBE] probe_dapi.py: forza UTF-8 su stdout/stderr per console Windows` (fix encoding cp1252, +8 righe, nessuna modifica alla logica del probe).
- Untracked irrilevante: `.claude/scheduled_tasks.lock` (lock locale, NON versionare).

## Cosa è già stato fatto stanotte (29/05 ~01:30-01:50)
1. Letto il handoff, confermato i 6 fatti DAPI.
2. `git status` / `git log` → allineati a `9bf35fa`, poi committato fix UTF-8 → ora `2dc457b`.
3. **Sanity OK** (verde): banner `CONN_OK;TRUE` su 10001 e 10003; `FIB6F` e `DITAS` daily → `candles=8 end=True errs=0`. ⚠️ **RETTIFICA 2026-05-29 post-V-1**: il sanity dichiarava qui sotto "schema O;L;H;C;V riconfermato" — ma il "riconferma" era solo su L e H (gli unici distinguibili dai 4 valori daily). V-1 ha poi provato con tick realtime che lo schema reale è `C;L;H;O;V`. La parte daily ri-verificata qui (FIB6F 19/05: 48429/48315/48980/48725) rimane coerente con entrambi gli schemi sulle posizioni 2,3 (L=48315 min, H=48980 max); le posizioni 1 e 4 (48429 e 48725) erano O e C ma in ordine inverso da quanto allora si pensasse — non era un errore di osservazione, era il limite strutturale del test daily. Vedi `tasks/PROBE_RECUPERO_GAP_DAPI.md` §3 e §7.
4. **Re-run inventory CME tentato → APPENDED_ROWS=0 (NON è un bug)**: probe diretta CANDLERANGE conferma che il DAPI ferma i 6 CME (`CM.MESM6/U6`, `CM.MNQM6/U6`, `CM.MYMM6/U6`) al **2026-05-27**; il settle 28/05 non è ancora propagato. FIB6F/DITAS invece arrivano al 28/05.

## Cosa fare oggi 29/05 (in ordine) — DUE note operative decise a voce, non ancora altrove

### 1. V-1 cattura morning — alle 09:00, LO LANCIA IL SUPERVISORE
Il supervisore preferisce lanciarlo lui alle 09:00 (non tenere processi appesi tutta la notte). Prima rifare il sanity (le condizioni notturne cambiano):
```powershell
cd C:\Users\AN\Documents\Projects\ga-zone-engine
python scripts/probe_dapi.py sanity
python scripts/probe_dapi.py v1-capture --window morning --tickers FIB6F,DITAS
```
Vincoli D-6: DGo+Darwin aperti, una sola istanza Darwin, **TradingView Directa CHIUSO**.
29/05 è venerdì → mercati aperti, flusso realtime vivo.

### 2. Re-run inventory CME — nel POMERIGGIO (dopo le 14:30 CET)
Il settle CME 28/05 dovrebbe propagarsi più tardi. Rilanciare allora:
```powershell
python scripts/update_inventory_indici_futures_daily.py
```
Atteso: recupera 28/05 (ed eventualmente 29/05 europeo) per i 6 CME.

### 3. Poi, da handoff: v1-capture afternoon (14:30), v1-fetch --date 2026-05-29, v1-compare, V-2 cut-off.

## Output finale atteso (invariato dal handoff)
Comporre `tasks/PROBE_RECUPERO_GAP_DAPI.md` con risultati V-1 e V-2 + commit/push. **NON aprire CAP-DATA-03.**
