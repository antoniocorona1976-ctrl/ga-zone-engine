# REPORT SUPERVISORE - CAP-DATA-02 (Parte 9) - Iterazione 1

**Task**: Pipeline runtime FIB su Directa DAPI - scrittura iniziale capitolo (12 sezioni Cap.45-56)
**Stato**: COMPLETATO

## Cosa e stato prodotto

- `docs/methodology_v2/CAP_09_parte_9.md` - nuovo capitolo metodologico Parte 9 con 12 sezioni normative (Cap.45-56). Contenuti: premessa e collocazione (Cap.45); architettura canale DAPI gateway Darwin con banner `Release 2.5.1`, triplo porte 10001/10002/10003, `APIPortSettings.txt` come identificatore locale, vincolo uso esclusivo D-6, pattern socket persistente, rate-limit empirico (Cap.46); catalogo simboli FIB front-month e cash europei con schemi `ANAG/BOOK_5/PRICE` e codice mese Directa-IDEM `I=settembre` verificato via ANAG su FIB6I (Cap.47); format dati canonico CSV BOM UTF-8 piu' manifest JSON, dominio `source` IN {DIRECTA, AGG_FROM_60s, AGG_FROM_D} (Cap.48); tabella canonica mappatura schema DAPI -> bundle frozen Portara per tutti i campi `bar_open / bar_high / bar_low / bar_close / volume / tick_count / bar_synthetic` con regola `bar_synthetic=True` simmetrica con Parte 8 Cap.40 (Cap.49); decodifica codici errore DAPI `1004 / 1007 / 1030` empiricamente verificati, backoff esponenziale, riavvio Darwin mezzanotte con marker `RUNTIME_GAP_START/END`, stato `RUNTIME_DEGRADED` su conflitto DGo (Cap.50); warm-up degli stati condizionali via DAPI lookback minore o uguale a 30 giorni, fallback Portara per downtime maggiore di 100 giorni (Cap.51); sessione operativa 08:00-22:00 CET allineata a epoca E5 di Parte 8 Cap.41, policy timezone CET/CEST con conversione automatica (Cap.52); decisione Q-A-3 ratificata su gating cash europei come logging piu' gating qualitativo configurable fuori dal GA con perimetro vincolante esplicito (Cap.53); audit log JSON Lines append-only, retention 90gg rolling piu' retention permanente sui giorni di emissione segnale, soglia commissioni minore di 200 EUR/mese tollerata (Cap.54); punti aperti fuori scope con rinvio esplicito a CAP-DATA-03 / Parte 10 (Cap.55); tabella decisioni 17 voci D-9-1..D-9-17 con criteri di rollback (Cap.56).
- `docs/methodology_v2/00_indice.md` - aggiunta sezione "Parte 9 - Pipeline runtime FIB su Directa DAPI (~8-10 pp) [IN REVIEW]" con lista dei 12 capitoli (Cap.45..Cap.56). Nessuna modifica ai blocchi Parti I-VIII preesistenti.
- `reports/REPORT_CAP_09.md` - questo report supervisore.

## Ipotesi di partenza

Il comportamento del motore che la Parte 9 intende influenzare e' la **continuita' operativa fra training storico Portara e inference real-time DAPI**, preservando l'invariante `research semantics = runtime semantics`. Senza questa formalizzazione, il bundle frozen calibrato sul tape Portara potrebbe vedere in runtime barre 1-min con schema diverso (es. flag `bar_synthetic` non propagato, timezone divergente, mancata gestione del riavvio mezzanotte Darwin), introducendo silenziosamente bias di distribuzione e contaminando l'inference. La pipeline runtime CON la convenzione di Parte 9 garantisce che il bundle non veda mai dati DAPI grezzi ma uno schema normalizzato simmetrico al preprocessor di Parte 8 Cap.40, e che gli stati condizionali EGARCH / regime / normalizzazione siano warmed-up correttamente a ogni avvio sessione (incluso post-riavvio mezzanotte Darwin). L'impatto atteso sul ranking GA dei cromosomi non e' diretto (la Parte 9 non modifica il walk-forward Parte V o la fitness Parte V Cap.24), e' indiretto: la **conversione signal-to-trade** in produzione e la **stabilita' delle metriche live** della dashboard di Parte VI Cap.30 dipendono dal fatto che il flusso DAPI sia normalizzato e che gli stati condizionali siano coerenti con il bundle frozen.

La decisione Q-A-3 (gating cash europei come logging piu' gating qualitativo configurable fuori dal GA) e' calibrata sul vincolo di non perturbare il GA: il cash europeo NON entra nel feature tensor, NON entra nella state machine, NON entra nel cromosoma. La modifica vive in `config/gating_rules.yaml` versionato, modificabile senza re-training. Il razionale: aprire il gating cash dentro il GA avrebbe richiesto re-validazione walk-forward di tutti i fold (cambio strutturale del payload e della state machine, carico Review elevato); aprire il gating cash come feature avrebbe rotto la fasizzazione PHASE-1 FIB-only di Parte 8 Cap.42 e richiesto vendor cross-index extra. Q-A-3 sblocca il valore "intermarket live" zero-cost (cash europei gratuiti DAPI base) senza vincolare il GA.

## Decisioni rilevanti prese durante lo sviluppo

1. **Numerazione capitoli interni Cap.45-Cap.56**: vincolante da sezione (ii) di ACTIVE_TASK.md. Applicata letteralmente. Parte 9 e' nuovo blocco normativo posto in coda alla Parte 8 (Cap.37-44).
2. **Citazione ereditarieta' Parti I-VIII**: ogni sezione cita esplicitamente i capitoli ereditati nella forma "Parte X Cap.YY" stabilita dal Planner (es. "Parte 8 Cap.40", "Parte III Cap.13", "Parte V Cap.22"). Nessuna duplicazione di contenuti gia' normati: solo rinvii puntuali con riferimento al numero di capitolo.
3. **Tabella mappatura schema DAPI -> bundle frozen Portara (Cap.49)**: coperti **tutti** i campi del bundle frozen di Parte 8 Cap.40 (`bar_open / bar_high / bar_low / bar_close / volume / tick_count / bar_synthetic`), piu' `timestamp` come chiave temporale. Il campo `tick_count` non e' esposto dallo schema `CANDLE` documentato DAPI (campi 1-9): la regola di derivazione e' esplicita (aggregazione tick da push `PRICE` in regime realtime; proxy `volume` o `NaN` con marker su record di warm-up storico). La distinzione fra regime realtime e regime storico e' coperta dal flag `bar_synthetic`.
4. **Decisione Q-A-3 ratificata con perimetro vincolante esplicito (Cap.53)**: 4 vincoli normativi esplicitati - NO feature tensor, NO state machine, NO cromosoma, NO walk-forward. Tabella razionale Q-A-1 / Q-A-2 / Q-A-3 con esito esplicito (Q-A-3 ratificata). Rollback non richiede re-training del bundle: il gating vive in `config/gating_rules.yaml` versionato e fuori dal genoma.
5. **6 Gap chiusi esplicitamente**:
   - Gap-1 (autenticazione locale) -> Cap.46: `APIPortSettings.txt` come identificatore locale single-account per macchina, non credenziale; loopback `127.0.0.1` only.
   - Gap-2 (timezone CET/CEST) -> Cap.52: doppia rappresentazione (ora locale CET/CEST nei CSV/manifest, UTC nei log di audit), conversione automatica DST europea.
   - Gap-3 (riavvio Darwin mezzanotte) -> Cap.50: marker `RUNTIME_GAP_START` / `RUNTIME_GAP_END`, backoff esponenziale, re-warm-up post-restart.
   - Gap-4 (audit log retention) -> Cap.54: 90 giorni rolling minima piu' retention permanente sui giorni di emissione segnale.
   - Gap-5 (test regressione exports) -> Cap.48: gate operativo pre-rilascio, diff byte-per-byte sui sample committati.
   - Gap-6 (soglia commissioni 200 EUR) -> Cap.54: DAPI base 20 EUR/mese tollerato (non e' "market data a pagamento" nel senso di D-1, e' costo strutturale del runtime), notifica supervisore via Telegram.
6. **Punti aperti fuori scope espliciti (Cap.55)**: FDAX standard non abilitato `B6086`, lookup completa codici mese Directa-IDEM, vendor cross-index pluriennale, continuita' tape / recupero gap / riconciliazione canonica giornaliera / storicizzazione strutturata (rinvio esplicito a CAP-DATA-03 / Parte 10), M-2 OPEN su latenza Telegram come carryover ad Appendice E.

## Misura prima/dopo

| Metrica | Prima | Dopo | Delta |
|---------|-------|------|-------|
| Capitoli metodologici v2 esistenti | 44 (Parti I-VIII, Cap.1-44) | 56 (Parti I-IX, Cap.1-56) | +12 capitoli Parte 9 |
| Gap normativi sulla pipeline runtime DAPI documentati | 0 (canale non formalizzato in doc v2) | 17 decisioni D-9-1..D-9-17 piu' 6 Gap chiusi (Gap-1..Gap-6) | +17 decisioni piu' 6 Gap |
| Coerenza adapter DAPI -> bundle frozen Portara | informale / da indagini | normata in tabella canonica Cap.49 per tutti i campi del bundle | normata |
| Decisione Q-A (gating cash europei) | aperta (Q-A-1 / Q-A-2 / Q-A-3 candidate) | Q-A-3 ratificata con perimetro vincolante (no feature, no state machine, no cromosoma, no walk-forward) | chiusa |
| Punti aperti rinviati a CAP-DATA-03 / Parte 10 | non tracciati | 4 esplicitati (continuita' tape, recupero gap, riconciliazione canonica, storicizzazione strutturata) piu' 3 esterni (FDAX standard, lookup IDEM, vendor cross-index) | +7 tracciati |

## Verifica esplicita degli Acceptance Criteria

Acceptance Criteria estratti da `tasks/ACTIVE_TASK.md` sezione "Done criteria (verificabili)".

| AC-ID | Criterio (estratto da ACTIVE_TASK) | Esito | Evidenza (file:riga) |
|-------|------------------------------------|-------|----------------------|
| AC-1 | File `docs/methodology_v2/CAP_09_parte_9.md` prodotto, 8-10 pp (stima), struttura coerente con la scaletta in calce (12 sezioni) | OK | `docs/methodology_v2/CAP_09_parte_9.md` capitoli Cap.45-Cap.56 (12 sezioni) presenti |
| AC-2 | File `reports/REPORT_CAP_09.md` prodotto secondo template supervisore | OK | `reports/REPORT_CAP_09.md` (questo file) |
| AC-3 | Tutti gli input autoritativi della sezione INPUT verbatim nel capitolo: banner Darwin "Release 2.5.1", comandi CANDLERANGE/SUB/UNSUB, schemi CANDLE/ANAG/BOOK_5/PRICE, codici errore 1004/1007/1030, limite 100gg, rate-limit ~14 connessioni / cooldown 30s, conflitto DGo | OK | banner `Release 2.5.1` citato (CAP_09 Cap.46 paragrafo "Banner del gateway Darwin"); CANDLERANGE (Cap.46, Cap.48, Cap.50, Cap.51); SUB/UNSUB (Cap.46, Cap.47, Cap.50); CANDLE schema (Cap.49 tabella); ANAG (Cap.47 con esempio reale `ANAG;FIB6I;14:05:30;IT0024847870;...`); BOOK_5 (Cap.47 con esempio reale); PRICE (Cap.47 con esempi reali `dGER`/`dITAS`); codici 1004/1007/1030 (Cap.50 tabella decodifica); limite 100gg (Cap.46, Cap.48, Cap.51, Cap.55); rate-limit empirico (Cap.46 paragrafo "Rate-limit osservato": 26 comandi sequenziali su connessione persistente, cooldown ~30s dopo 14 aperture TCP rapide); conflitto DGo (Cap.46 "Vincolo uso esclusivo" piu' Cap.50 "Stato RUNTIME_DEGRADED") |
| AC-4 | Tabella mappatura schema DAPI -> bundle frozen Portara completa per TUTTI i campi del bundle (bar_open, bar_high, bar_low, bar_close, volume, tick_count, bar_synthetic) | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.49 tabella canonica copre 8 righe: `timestamp`, `bar_open`, `bar_high`, `bar_low`, `bar_close`, `volume`, `tick_count`, `bar_synthetic` (tutti i campi del bundle frozen Portara di Parte 8 Cap.40 con regola di derivazione esplicita) |
| AC-5 | Decisione Q-A chiusa nel testo con verdetto piu' motivazione piu' perimetro operativo (Q-A-3 ratificata) | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.53 con verdetto esplicito "Ratificata Q-A-3", tabella razionale Q-A-1 / Q-A-2 / Q-A-3, perimetro vincolante (4 NO: feature tensor, state machine, cromosoma, walk-forward), regola operativa `config/gating_rules.yaml`, coerenza con Parte 8 Cap.42 e Parte 8 Cap.37 |
| AC-6 | Tutti i 6 Gap (Gap-1..Gap-6) chiusi nel capitolo | OK | Gap-1 chiuso in Cap.46 paragrafo "Gap-1 - Autenticazione canale"; Gap-2 chiuso in Cap.52 paragrafo "Gap-2 - Policy timezone CET/CEST"; Gap-3 chiuso in Cap.50 paragrafo "Gap-3 - Riavvio Darwin mezzanotte"; Gap-4 chiuso in Cap.54 paragrafo "Gap-4 - Retention policy"; Gap-5 chiuso in Cap.48 paragrafo "Gap-5 - Test regressione exports"; Gap-6 chiuso in Cap.54 paragrafo "Gap-6 - Soglia commissioni" |
| AC-7 | Tabella decisioni del capitolo (ID, descrizione, motivazione 1 riga) presente | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.56 tabella con 17 decisioni D-9-1..D-9-17 (colonne ID, Decisione, Motivazione) piu' criteri di rollback registrati |
| AC-8 | Sezione "Punti aperti fuori scope" con almeno: abilitazione FDAX standard, lookup completa codici mese IDEM (oltre I=settembre), vendor cross-index pluriennale, continuita'/storicizzazione (rinvio a CAP-DATA-03) | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.55 elenca: abilitazione FDAX standard (Mini-DAX e Micro-DAX abilitati, FDAX standard ERR;1007 verificato 2026-05-27); lookup completa codici mese Directa-IDEM (I=settembre verificato, altri F/J/L/M/O attesi da derivare); vendor cross-index pluriennale (FirstRateData/Kibot/IQFeed/Databento/Polygon candidati); continuita' tape / recupero gap / riconciliazione canonica giornaliera / storicizzazione strutturata rinviati a CAP-DATA-03 / Parte 10 con rinvio esplicito; M-2 OPEN su latenza Telegram come carryover ad Appendice E; persistenza dati DAPI per re-training fuori scope; implementazione codice operativo (FASE-D del roadmap) |
| AC-9 | 00_indice.md aggiornato a fine ciclo (Parte 9 = PASS con hash review) | PARZIALE | A inizio Iterazione 1 (questa) l'indice e' aggiornato a "Parte 9 ... [IN REVIEW]". L'aggiornamento a "PASS con hash review" e' eseguito dall'Orchestratore al PASS della Review, non dal Developer; pertanto in questa iterazione lo stato corretto e' "IN REVIEW", coerente con la sequenza di chiusura sessione PASS (7 condizioni, vedi `.claude/CLAUDE.md`). |
| AC-10 | Nessuna modifica a Parti I-VIII del doc v2 | OK | Le righe 1-83 di `docs/methodology_v2/00_indice.md` (blocchi Parti I-VIII piu' Appendici operative pre-esistenti) sono invariate; la modifica e' esclusivamente l'inserimento di un blocco Parte 9 in coda prima delle Appendici operative. Verifica diretta via `git diff` 00_indice.md mostra solo aggiunta. |

## Domande aperte per il Planner

Nessuna. Il task card e' stato eseguito senza ipotesi proprie del Developer: tutti i 10 Done criteria sono coperti (AC-9 ha onestamente lo stato corretto "IN REVIEW" per questa iterazione, l'aggiornamento a PASS e' compito dell'Orchestratore al verdetto Review). Decisioni ratificate (D-1, D-6, C-1..C-7, Q-A-3) applicate verbatim. Carryover (M-2 OPEN ad Appendice E, M-16 OPEN-CONDIZIONALE Parte VII) invariati. Sei Gap chiusi. Punti aperti fuori scope rinviati a CAP-DATA-03 / Parte 10 con rinvio esplicito.

## Criterio di rollback

- Decisione D-9-14 (Q-A-3, gating cash europei come logging piu' gating qualitativo configurable fuori dal GA) **reversibile** in capitolo successivo (CAP-DATA-03 / Parte 10 o futuri cicli) se l'esperienza operativa mostra che il logging cash e' inutile (nessun evento osservato in 90 giorni rolling) o dannoso (false-positive eccessivi sul gating maggiore del 10% degli alert generati). Il rollback non richiederebbe re-training del bundle: basterebbe disattivare le sottoscrizioni cash europee dalla pipeline runtime e svuotare il file `config/gating_rules.yaml`. Nessuna perturbazione del GA, del feature tensor, della state machine o del cromosoma.
- Decisione D-9-4 (tabella ticker DAPI canonical e codice mese `I=settembre`) **non reversibile** (fattuale, verificato empiricamente 2026-05-27 via ANAG su FIB6I `IT0024847870` descrizione `FTSE MIB INDEX FUTURE SET26`).
- Decisione D-9-7 (adapter `bar_synthetic` in runtime simmetrico con Parte 8 Cap.40) **non reversibile** (invariante semantico ereditato; cambio violerebbe la regola research = runtime di Parte 8 Cap.37).
- Decisione D-9-10 (vincolo D-6 uso esclusivo canale, no DGo / TradingView concorrente) **non reversibile** per la durata della sessione runtime corrente; revisione richiederebbe nuovo task Planner.
- Criterio generico di rollback Iterazione 1 -> Iterazione 0 (rimozione del capitolo Parte 9 dal doc v2): se la Review v1 emette FAIL bloccante con piu' di 5 BUG REALI non risolvibili in iterazione di rework v2 chirurgica, il Planner valuta se rifare il task da zero (rollback a stato pre-CAP-DATA-02). In tal caso `docs/methodology_v2/CAP_09_parte_9.md` viene cancellato, `docs/methodology_v2/00_indice.md` torna allo stato pre-Parte 9 (righe 1-83 invariate, niente blocco Parte 9 in coda), `reports/REPORT_CAP_09.md` archiviato come storico. Probabilita' di questo rollback: bassa, dato che le decisioni del task card sono ratificate dal Planner e i dati empirici (2026-05-27) sono fattuali.

---

## Iterazione 2 (Rework v2) — risposta ai finding di Review v1

**Verdetto Review v1**: FAIL (commit `baeab2c`, file `reviews/REVIEW_CAP_09_review.md`).
**Punto di controllo supervisore**: eseguito 2026-05-28. Decisioni del supervisore: 7 BUG REALI (B-1..B-7) obbligatori; 4 MIGLIORA PERFORMANCE (NB-1..NB-4) tutti decisi Opzione A obbligatori; NB-5 si chiude per propagazione da B-2; NB-6, O-1, O-2 NEUTRO ignorati per default CLAUDE.md.

### Rework v2 — finding chiusi (mappatura puntuale)

| Finding | Tipo | Capitolo / Riga (post-rework) | Citazione testo nuovo (sintesi) | Riferimento incrociato Parti I-VIII |
|---------|------|-------------------------------|----------------------------------|--------------------------------------|
| **B-1** | BUG REALE | Cap.52 paragrafi "State machine del segnale e finestra di sessione" e "Apertura/Chiusura sessione" | Rimossa la regola di chiusura automatica a 22:00 CET. Il segnale `active` alla chiusura sessione **resta `active`**: il counter $\Delta t_{cromosoma}$ (Parte II Cap.7 r126, dominio fino a 1680 minuti) **scavalca le interruzioni notturne**; la transizione e' triggerata esclusivamente dal counter cromosoma-specifico, mai dall'orologio sessione. La pipeline runtime fuori sessione e' in stand-by ma mantiene lo stato del segnale in memoria persistente; al boot del giorno successivo riprende dallo stato salvato. | Parte II Cap.7 r126 (counter $\Delta t_{cromosoma}$, dominio scavalca notte); Parte II Cap.10 (replay deterministico bit-exact); Parte VI Cap.30 (metriche lifecycle live non inquinate da chiusure spurie); Parte 8 Cap.41 (epoche di sessione FIB E5). |
| **B-2** | BUG REALE | Cap.48 paragrafi "Schema CSV BOM UTF-8 (format runtime normativo)", "Pipeline runtime su griglia 1-min uniforme", "Distinzione fra format runtime normativo e sample di validazione (Gap-5)" + paragrafo Gap-5 aggiornato | Header CSV runtime esteso con `tick_count` e `bar_synthetic`; sample CSV con due righe (una reale, una sintetica con `bar_synthetic=True`, `tick_count=0`, `volume=0`). La pipeline produce griglia 1-min **uniforme** sulla sessione 08:00-22:00 CET. Sample legacy (`data/runtime/exports_sample/`) dichiarati esplicitamente **input campione di validazione del Gap-5**, non template normativo del format runtime; lo script storico legacy non e' aggiornato in CAP-DATA-02 per non rompere riproducibilita' del Gap-5 (rivisitazione rinviata a CAP-DATA-03). Coerenza con bundle frozen di Parte 8 Cap.40 r94 ripristinata. | Parte 8 Cap.40 r94 (`tick_count` e `bar_synthetic` persistiti nel bundle frozen); Parte III Cap.13 (EGARCH input — solo barre `bar_synthetic=False`); Parte III Cap.14 (classificazione regime su quantili rolling — input non contaminato da forward-fill). |
| **B-3** | BUG REALE | Cap.49 tabella canonica riga `bar_synthetic` + paragrafo "Regola `bar_synthetic = True` in runtime (B-3, B-7)" | Regola riformulata e distinta per 3 regimi del flusso DAPI: (a) **FIB realtime push `BOOK_5`** (porta 10001): barra reale se almeno un evento `BOOK_5` con `bid1_lots ≥ 1` AND `ask1_lots ≥ 1` nel minuto, altrimenti sintetica con forward-fill del mid level-1; (b) **FIB storico `CANDLERANGE`** (porta 10003): barra reale se compare nella risposta `CANDLE`, altrimenti sintetica con forward-fill su Close (regola Parte 8 Cap.40); (c) **Cash europei push `PRICE`** (porta 10001): legacy — sintetica se nessun `PRICE` nel minuto. Eliminato il presupposto erroneo "push `PRICE` su FIB"; motore non piu' inerte. | Parte 8 Cap.40 (forward-fill su Close, `volume=0`, `bar_synthetic=True` per minuti senza trade); Parte III Cap.13 (EGARCH su barre `bar_synthetic=False`); Parte VII Cap.31 (replay deterministico, gap semantics). |
| **B-4** | BUG REALE | Cap.47 tabella simboli FIB + nuovo paragrafo "Nota normativa sulla differenza di schema futures vs cash" | Tabella futures FIB corretta: rimosso `PRICE` dalla colonna "Schema realtime"; restano `BOOK_5, ANAG`. Per FIB6I verifica empirica 2026-05-27 (SUB restituisce solo `ANAG` + `BOOK_5`, mai `PRICE`); per FIB6F, MINI6x atteso per simmetria. Cash europei (DGER, DSTX50, DITAS, DFRA) mantengono `PRICE`. Nuova nota normativa giustifica la differenza: per futures IDEM il gateway pubblica book come tick stream, per cash europei last price come tick stream. La pipeline runtime adotta 2 strategie di costruzione barre 1-min (book-based per FIB, last-based per cash). | Parte 8 Cap.42 (cross-index PHASE-2 — i futures FIB non sono cross-index, sono lo strumento di calibrazione); empirismo `tasks/INDAGINE_DIRECTA_CROSS_INDEX.md` Appendice B (probe 2026-05-27). |
| **B-5** | BUG REALE | Cap.53 paragrafi "Verdetto Q-A-3 (ratificato)", "Vita operativa del gating qualitativo (B-5, post-emissione)", "Replay deterministico bit-exact preservato (B-5)" + tabella razionale Q-A-1/Q-A-2/Q-A-3 aggiornata | Gating cash riformulato come **POST-EMISSIONE**: il segnale e' **sempre emesso** dalla state machine (Parte II Cap.7), sempre tracciato in audit (Cap.54, marker `SIGNAL_EMITTED`), sempre conteggiato nelle metriche di lifecycle live (Parte VI Cap.30). Il gating opera **esclusivamente** sul payload del messaggio Telegram come **annotazione/nota di avvertimento** (es. `[GATING-cash-europei: DGER -2.3% intraday]`). Replay bit-exact preservato: stesso event log `SIGNAL_EMITTED` con stesso payload strutturale a parita' di tape DAPI, indipendentemente dalla config `gating_rules.yaml`. Eliminata la confusione funzionale con Q-A-2 (gating-soppressione). | Parte II Cap.7 (state machine emette indipendentemente da gating qualitativo); Parte II Cap.10 (replay deterministico bit-exact sull'event log del segnale); Parte VI Cap.30 (metriche lifecycle live invariate, conteggiano i segnali emessi non i messaggi pubblicati); Parte VII Cap.35 (bundle frozen immutato). |
| **B-6** | BUG REALE | Cap.51 paragrafo "Limite 100 giorni e stato `RUNTIME_STALE_RESTART` (B-6)" | Rimosso il fallback Portara per downtime > 100 giorni. Sostituito con: la pipeline runtime **non riparte automaticamente** se downtime > 100 giorni; entra in stato `RUNTIME_STALE_RESTART` (marker audit Cap.54) e richiede intervento supervisore. Riconciliazione DAPI/Portara e re-bootstrap dopo gap > 100gg sono fuori scope CAP-DATA-02, rinviati a CAP-DATA-03 / Parte 10. Probabilita': bassa (richiede downtime > 5 mesi solari). Bundle EGARCH calibrato su Portara ratio-adjusted non viene mai eseguito su warm-up cross-source mescolato. Dipendenza circolare con CAP-DATA-03 eliminata. | Parte 8 Cap.37 (Portara ratio-adjusted come fonte ufficiale di training); Parte 8 Cap.38 (back-adjustment ratio-adjusted vs unadjusted nativo del DAPI runtime); Parte III Cap.13 (EGARCH calibrato sul tape ratio-adjusted, no esecuzione cross-source). |
| **B-7** | BUG REALE | Cap.49 tabella canonica riga `tick_count` + paragrafo "Regola `bar_synthetic = True` in runtime (B-3, B-7)" | Mappatura `tick_count` riformulata distinguendo correttamente i due assi: (a) **realtime** (porta 10001): `tick_count` = numero di eventi `BOOK_5` ricevuti nel minuto (proxy puntuale dell'attivita' di book); (b) **storico** (porta 10003 `CANDLERANGE`): `tick_count` non esposto dallo schema `CANDLE` -> impostato a `NULL` (marker assente, non `NaN` numerico). Il campo `bar_synthetic` resta **booleano (trade vs no-trade)**, come in Parte 8 Cap.40, e NON discrimina realtime/storico. Il discriminante realtime/storico e' la porta sorgente del record, registrata in audit. Consumer a valle (feature engineering Parte III Cap.15) configurato per ignorare `NULL` o usare proxy `volume`. | Parte 8 Cap.40 (`bar_synthetic` booleano trade/no-trade); Parte III Cap.15 (catalogo 37 feature, configurazione consumer su `tick_count`/`volume`); coerenza con la riga `tick_count` del bundle frozen Portara. |
| **NB-1** | MIGLIORA PERFORMANCE (Opzione A) | Cap.47 esempi schemi ANAG/BOOK_5 + Cap.55 paragrafo "Lookup completa codici mese Directa-IDEM" | Esempi normativi schemi ANAG/BOOK_5 spostati su **FIB6F** come ticker normativo del front-month corrente (Q2 2026); il probe empirico 2026-05-27 su FIB6I e' citato come verifica reale del next-month. Schema ANAG atteso per FIB6F: descrizione `FTSE MIB INDEX FUTURE GIU26` con `GIU26 = giugno 2026`. In Cap.55 lookup parziale **gia' nota** dichiarata esplicitamente: `F = giugno` (dai sample export FIB6F Q2 2026) e `I = settembre` (dal probe ANAG FIB6I 2026-05-27); lookup completa per gli altri codici resta runtime-discovery in FASE-D. | Parte 8 Cap.42 (cross-index PHASE-2 dichiarata ma non implementata); pratica esistente sample `data/runtime/exports_sample/FIB6F_20260107_20260402`. |
| **NB-2** | MIGLIORA PERFORMANCE (Opzione A) | Cap.47 nuovo paragrafo "Policy switch front-month durante runtime (NB-2 Opzione A)" + correzione Cap.48 paragrafo back-adjustment + tabella decisioni Cap.56 voce D-9-NB2 | Aggiunto paragrafo dedicato in Cap.47 che norma: contratto FIB scade **terza venerdi del mese alle 09:00 CET**; switch operativo pipeline al **boot della sessione del giorno $t$** (terza venerdi): pipeline alle 08:00 CET sottoscrive direttamente next-month saltando finestra 08:00-09:00 del front in scadenza. Marker `CONTRACT_SWITCH` in audit con payload `{from: "FIB6F", to: "FIB6I", scadenza_from: "2026-06-19"}`. Distinzione esplicita rispetto al filtro pre-expiry $N=3$ di **training** (Parte 8 Cap.39): le due regole non sono sovrapposte. Tabella decisioni Cap.56 aggiornata con D-9-NB2 e relativo rollback (richiede nuovo task Planner). | Parte 8 Cap.38 (ratio-adjusted virtuale assorbe la continuita' del prezzo attraverso il roll); Parte 8 Cap.39 (filtro pre-expiry $N=3$ di training, distinto); Parte III Cap.13 (EGARCH cross-session preservato sulla serie virtuale continua). |
| **NB-3** | MIGLIORA PERFORMANCE (Opzione A) | Cap.54 paragrafi "Tipologia evento" + "Granularita' eventi del lifecycle del segnale (NB-3 Opzione A)" + paragrafo retention permanente + tabella decisioni Cap.56 voce D-9-NB3 | Sostituito evento aggregato `SIGNAL_CLOSED` con sei eventi terminali distinti coerenti con la state machine Parte II Cap.7: `SIGNAL_TARGET_1_HIT`, `SIGNAL_STOPPED`, `SIGNAL_INVALIDATED`, `SIGNAL_MISSED_TARGET`, `SIGNAL_EXPIRED`, `SIGNAL_REVOKED`. Mantenuti pre-terminali `SIGNAL_EMITTED`, `SIGNAL_TRIGGERED`. Per `SIGNAL_MISSED_TARGET` sotto-distinzione `pretrigger`/`posttrigger` nel payload JSON con campo obbligatorio `timeout_cause`. Dashboard live e replay bit-exact ricevono eventi puntuali sufficienti. Retention permanente estesa ai 6 eventi terminali. Tabella decisioni Cap.56 aggiornata con D-9-NB3 e relativo rollback (reversibile in capitolo successivo). | Parte II Cap.7 (6 stati terminali distinti: `target_1_hit`, `stopped`, `invalidated`, `missed_target` con causa `pretrigger_timeout`/`posttrigger_timeout`, `expired`, `revoked`); Parte II Cap.10 (replay deterministico bit-exact); Parte VI Cap.30 (dashboard live calcola target_hit_rate, invalidation_rate, miss_rate per causa). |
| **NB-4** | MIGLIORA PERFORMANCE (Opzione A) | Cap.51 punto 2 della procedura warm-up + tabella decisioni Cap.56 voce D-9-NB4 + sintesi aritmetica nei criteri di rollback | $L_{warmup} = 30$ giorni di trading IDEM **congelato in Parte 9** (NB-4 Opzione A). Motivazione aritmetica esplicita: requisito minimo $N_{reg} = 20$ sessioni (Parte III Cap.14) + ~50% margine. Implementazione: $30 \times 840 = 25.200$ barre 1-min copre $W_{norm}=1000$ (Parte III Cap.15), $T_{warmup,\text{EMA}}=74$ (Parte III Cap.15), $T_{warmup,\text{norm}}=100$ (Parte III Cap.15), $N_{reg} \cdot 840 = 16.800$ con margine residuo $\sim 8.400$ barre. Rifinibile solo via nuovo task Planner (non reversibile dentro Parte 9; replay bit-exact ripristinato). Tabella decisioni Cap.56 aggiornata con D-9-NB4 e relativo rollback. | Parte III Cap.13 (EGARCH cross-session, osservazione coda bassa $N=5$, stabilizzazione su residui $\sim 8.400$); Parte III Cap.14 ($N_{reg}=20$, quantile $p=0{,}75$, persistenza minima $T_{persist}=10$); Parte III Cap.15 ($W_{norm}=1000$, $T_{warmup,\text{EMA}}=74$, $T_{warmup,\text{norm}}=100$); Parte II Cap.10 (replay deterministico bit-exact). |

### Finding NON modificati (registrati per completezza)

- **NB-5** (CSV runtime senza colonna `bar_synthetic`): chiuso per propagazione da B-2 (header CSV runtime esteso con `tick_count` e `bar_synthetic`, sample uniforme). Verificato: il sample CSV normativo r109-114 di Cap.48 mostra esplicitamente una barra con `bar_synthetic=True` e `volume=0` distinta da una barra reale `bar_synthetic=False`, eliminando l'ambiguita' inferenziale di NB-5.
- **NB-6** (D-1 reinterpretato escludendo DAPI Datafeed base): NEUTRO per default CLAUDE.md; non modificato. Il paragrafo Gap-6 Cap.54 r361 mantiene la posizione del Developer originale.
- **O-1** (banner manifest sample con doppi spazi vs Cap.46): NEUTRO cosmetico; non modificato.
- **O-2** (DSTX50/DFRA dichiarati atteso lowercase senza verifica): NEUTRO; non modificato (la nota nella tabella Cap.47 distingue esplicitamente `verificato` per DGER/DITAS e `atteso lowercase` per DSTX50/DFRA, e' onesta).

### Misura prima/dopo (Rework v2)

| Metrica | Prima (v1 FAIL) | Dopo (v2) | Delta |
|---------|------------------|-----------|-------|
| BUG REALI aperti | 7 (B-1..B-7) | 0 | -7 |
| MIGLIORA PERFORMANCE aperti decisi | 4 (NB-1..NB-4) | 0 | -4 |
| Tabella decisioni Cap.56 | 17 voci D-9-1..D-9-17 | 20 voci (aggiunte D-9-NB2, D-9-NB3, D-9-NB4) | +3 |
| Eventi audit log distinti per lifecycle segnale | 3 (`SIGNAL_EMITTED`, `SIGNAL_TRIGGERED`, `SIGNAL_CLOSED`) | 8 (`SIGNAL_EMITTED`, `SIGNAL_TRIGGERED`, 6 terminali distinti) | +5 |
| Regola `bar_synthetic` (regimi distinti) | 1 generica (push tick / CANDLE indistinto) | 3 regimi distinti (FIB realtime, FIB storico, cash) | +2 |
| Schemi futures FIB nella tabella Cap.47 | 3 (`PRICE`, `BOOK_5`, `ANAG`) | 2 (`BOOK_5`, `ANAG`) | -1 (rimosso `PRICE` falso) |
| Fallback Portara warm-up >100gg | 1 (formalizzato Cap.51) | 0 (sostituito da stato `RUNTIME_STALE_RESTART`) | -1 |
| $L_{warmup}$ stato | ~30 valore di lavoro non congelato | 30 congelato in Parte 9 con motivazione aritmetica | congelato |
| Chiusura automatica segnali 22:00 CET | regola dichiarata | regola rimossa (state machine Parte II Cap.7 governa) | rimossa |

### Verifica esplicita Acceptance Criteria rework v2

Acceptance Criteria estratti dalla sezione "## Finding di Review da risolvere (rework v2)" di `tasks/ACTIVE_TASK.md`. Ogni AC corrisponde a un finding ratificato dal supervisore (7 BUG + 4 NB Opzione A = 11 AC).

| AC-ID v2 | Finding | Esito | Evidenza (file:riga post-rework) |
|----------|---------|-------|----------------------------------|
| AC-v2-B-1 | Rimuovere chiusura automatica segnali 22:00 CET | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.52 paragrafo "State machine del segnale e finestra di sessione" (r254-256) + paragrafo "Apertura/Chiusura sessione" (r264) |
| AC-v2-B-2 | Estendere header CSV runtime con `tick_count` e `bar_synthetic`; griglia 1-min uniforme; sample legacy = input campione validazione Gap-5 | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.48 paragrafi "Schema CSV BOM UTF-8 (format runtime normativo, B-2)" (r104-114), "Pipeline runtime su griglia 1-min uniforme" (r116), "Distinzione fra format runtime normativo e sample di validazione (Gap-5)" (r118) + paragrafo Gap-5 aggiornato (r129) |
| AC-v2-B-3 | Riformulare regola `bar_synthetic` per FIB futures basata su `BOOK_5` (realtime) o assenza in `CANDLERANGE` (storico); distinguere i due regimi | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.49 tabella canonica riga `bar_synthetic` + paragrafo "Regola `bar_synthetic = True` in runtime (B-3, B-7)" (r158-172) |
| AC-v2-B-4 | Rimuovere `PRICE` dai futures FIB nella tabella Cap.47; mantenere `PRICE` per cash; nota normativa giustifica la differenza | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.47 tabella simboli FIB (r66-72) + nuovo paragrafo "Nota normativa sulla differenza di schema futures vs cash" (r74) |
| AC-v2-B-5 | Gating cash POST-EMISSIONE come annotazione messaggio Telegram, MAI soppressione; replay bit-exact preservato | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.53 paragrafi "Verdetto Q-A-3 (ratificato)" (r294-296), "Vita operativa del gating qualitativo (B-5, post-emissione)" (r306-313), "Replay deterministico bit-exact preservato (B-5)" (r315) + tabella razionale Q-A aggiornata (r322-325) |
| AC-v2-B-6 | Rimuovere fallback Portara >100gg; sostituire con stato `RUNTIME_STALE_RESTART` + intervento supervisore; rinvio a CAP-DATA-03 | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.51 paragrafo "Limite 100 giorni e stato `RUNTIME_STALE_RESTART` (B-6)" (r236-240) |
| AC-v2-B-7 | Riformulare mappatura `tick_count` distinguendo realtime (count `BOOK_5`) vs storico (`NULL`); `bar_synthetic` resta booleano trade/no-trade | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.49 tabella canonica riga `tick_count` + paragrafo "Regola `bar_synthetic = True` in runtime (B-3, B-7)" (r158-172) |
| AC-v2-NB-1 | Esempi normativi su FIB6F (non FIB6I); descrizione ANAG attesa `GIU26 → giugno 2026`; in Cap.55 lookup parziale F=giu + I=set gia' nota | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.47 esempi schemi ANAG/BOOK_5 aggiornati (r92-93), Cap.55 paragrafo "Lookup completa codici mese Directa-IDEM" aggiornato (r381) |
| AC-v2-NB-2 | Paragrafo "Policy switch front-month durante runtime" in Cap.47; switch al boot giorno $t$ (terza venerdi); marker `CONTRACT_SWITCH`; esempio FIB6F→FIB6I al 2026-06-19; tabella D-9 aggiornata | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.47 paragrafo "Policy switch front-month durante runtime (NB-2 Opzione A)" (r97-107) + correzione coerente Cap.48 r143 + Cap.56 nuova voce D-9-NB2 (r419) |
| AC-v2-NB-3 | Sostituire `SIGNAL_CLOSED` con 6 eventi terminali distinti; per `MISSED_TARGET` sotto-distinzione `pretrigger`/`posttrigger` nel payload JSON; tabella D-9 aggiornata | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.54 paragrafi "Tipologia evento" aggiornato (r339), "Granularita' eventi del lifecycle del segnale (NB-3 Opzione A)" (r341), paragrafo retention permanente esteso (r358) + Cap.56 nuova voce D-9-NB3 (r420) |
| AC-v2-NB-4 | Congelare $L_{warmup}=30$ giorni di trading IDEM in Cap.51 con motivazione aritmetica; replay bit-exact ripristinato; tabella D-9 aggiornata | OK | `docs/methodology_v2/CAP_09_parte_9.md` Cap.51 punto 2 della procedura warm-up aggiornato (r221) + Cap.56 nuova voce D-9-NB4 (r421) + criteri di rollback aggiornati (r431) |

Tutti gli 11 finding ratificati dal supervisore sono chiusi onestamente con esito OK.

### Criterio di rollback (Iterazione 2)

- **B-1, B-3, B-4, B-5, B-7**: non reversibili (correzioni di coerenza con invarianti Parti I-VIII, no margine discrezionale).
- **B-2**: reversibile solo nella misura in cui il format CSV runtime potrebbe essere ulteriormente esteso (es. colonne aggiuntive per microstruttura book) senza rimuovere `tick_count` o `bar_synthetic`.
- **B-6**: reversibile in CAP-DATA-03 (la riconciliazione DAPI/Portara la' normata); non in Parte 9.
- **NB-2 (D-9-NB2)**: non reversibile dentro Parte 9; revisione policy switch richiede nuovo task Planner.
- **NB-3 (D-9-NB3)**: reversibile in capitolo successivo se la granularita' impoverita risulta sufficiente per dashboard e replay; il bundle frozen non e' impattato.
- **NB-4 (D-9-NB4)**: non reversibile dentro Parte 9; revisione $L_{warmup}$ richiede nuovo task Planner perche' impatta replay bit-exact.
- **NB-1**: reversibile (esempi normativi possono ruotare al cambio di front-month nel ciclo Q3/Q4 2026); il valore `F = giugno` e' fattuale, `I = settembre` empirico verificato.

Globale: la rework v2 e' un set di correzioni chirurgiche puntuali coerenti con i finding ratificati dal supervisore; nessuna riscrittura strutturale del capitolo. Se Review v2 emettesse FAIL, il Planner valuterebbe se i finding restano gli stessi o se sono nuovi; in entrambi i casi, l'iterazione v3 sarebbe ancora chirurgica.

---

## Micro-patch post-PASS (2026-06-02) — Cap.49 BOOK_5 bid1/ask1 → CERTIFICATE [W3/M-10]

**Natura**: micro-patch chirurgico di **sola annotazione di stato** su Parte 9 (PASS storico `86425a7`), NON una nuova iterazione del ciclo Planner→Developer→Reviewer. Aggiorna l'etichetta RM-1 sulle posizioni `BOOK_5` da cui dipende la regola `bar_synthetic` di Cap.49: da "Verifica parziale / non certificate / → Empirico-CLI" a "Verificato `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`".

### Motivazione

Quando Parte 9 fu scritta (PASS `86425a7`), lo schema `BOOK_5` derivava da una singola osservazione del 2026-05-27 (un solo evento FIB6I), con due alternative compatibili non escluse (ordine dei blocchi BID/ASK, indice del triplo `lots`/`orders`/`price`). La regola `bar_synthetic` era quindi correttamente etichettata "verifica parziale". Dopo la chiusura di Parte 9, l'audit empirico CLI **CAP-DATA-02 RM-RETRO** (fase CLI, mercato aperto 2026-06-01) ha **certificato** lo schema `BOOK_5` con prova diretta su 29 eventi / 290 triple del front-month liquido FIB6F. La certificazione e' ora disponibile e va riflessa nell'annotazione di Cap.49, che diversamente resterebbe sotto-dichiarata rispetto allo stato di conoscenza attuale.

### Fonte citata (autoritativa, NON ri-derivata)

- `reviews/REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md` §W3 (righe 55-73): `BOOK_5` = `[BID×5 best-first][ASK×5 best-first]`, ogni livello triplo `(lots, orders, price)`; **certificato bit-a-bit su 29 eventi / 290 triple FIB6F**. Posizioni (riga 66): `bid1_lots`=campo 4, `bid1_orders`=campo 5, `bid1_price`=campo 6; `ask1_lots`=campo 19, `ask1_price`=campo 21. Ordine BID-poi-ASK escluso invertito (blocco 1 sempre discendente, 29/29); triplo `(lots, orders, price)` confermato da `lots ≥ orders` su 290/290 (esclude `(orders, lots, price)`); `bid1_price < ask1_price` su 29/29; anomalia 27/05 `bid1>ask1` **NON riprodotta** (artefatto del campione illiquido FIB6I, non inversione di schema).
- `tasks/STATO_CORRENTE.md` §5 **M-10** (riga 77): stessa certificazione, "posizioni di `bar_synthetic` Cap.49 certificate".

**RM-1 onesto**: la fonte certifica le posizioni a **livello-1** (`bid1`/`ask1`) — esattamente i campi usati dalla regola `bar_synthetic` (mid level-1 e gate `bid1_lots≥1 AND ask1_lots≥1`). L'annotazione patchata dichiara "certificate a livello-1", non sovra-estende ai livelli 2-5 (non interrogati dalla regola). `ask1_orders` (campo 20) non e' enumerato esplicitamente dalla fonte e NON e' citato nelle nuove annotazioni.

### Prima → dopo (testuale)

**(A) Cap.49, riga tabella `bar_synthetic` (file `docs/methodology_v2/CAP_09_parte_9.md:173`)**

- PRIMA: "*(**Verifica parziale RM-1:** questa regola dipende dalle posizioni `bid1_lots`/`ask1_lots`/`bid1_price`/`ask1_price` nello schema `BOOK_5`, **non certificate** — lo schema deriva da osservazione singola 2026-05-27, alternative su ordine BID/ASK e indice `lots`/`price` non escluse, vedi Cap.47; se l'ordine dei livelli o l'indice del triplo fosse diverso, la regola si applicherebbe su campi sbagliati → Empirico-CLI.)*"
- DOPO: "*(**Verificato RM-1 `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`:** le posizioni `bid1_lots` (campo 4), `bid1_price` (campo 6), `ask1_lots` (campo 19) e `ask1_price` (campo 21) nello schema `BOOK_5` sono **certificate** a livello-1 — schema `[BID×5 best-first][ASK×5 best-first]`, ogni livello triplo `(lots, orders, price)`, confermato su 29 eventi / 290 triple su FIB6F front-month liquido `[rif. reviews/REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md, M-10]`. L'ordine dei blocchi (BID-poi-ASK) e' escluso essere invertito (blocco 1 sempre discendente = BID, 29/29 eventi); l'indice del triplo `(lots, orders, price)` e' confermato da `lots >= orders` su 290/290 triple (l'alternativa `(orders, lots, price)` e' esclusa). L'anomalia `bid1_price > ask1_price` del campione singolo 27/05, che a Cap.47 lasciava aperta l'ipotesi di blocchi invertiti, e' risultata **NON riprodotta** (artefatto del contratto illiquido FIB6I a scadenza lontana, non inversione di schema): `bid1_price < ask1_price` su 29/29 eventi. La regola `bar_synthetic` si applica quindi su campi certi. La cautela \"verifica parziale → Empirico-CLI\" di Cap.47 e' qui **saldata**.)*"

**(B) Cap.49, bullet prosa "Regola `bar_synthetic = True`" (file `docs/methodology_v2/CAP_09_parte_9.md:177`)**

- PRIMA: "*(**Verifica parziale RM-1:** dipende dalle posizioni `bid1_lots`/`ask1_lots` dello schema `BOOK_5`, non certificate — schema da osservazione singola 2026-05-27, alternative ordine BID/ASK e indice `lots`/`price` non escluse, vedi Cap.47 → Empirico-CLI.)*"
- DOPO: "*(**Verificato RM-1 `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`:** le posizioni `bid1_lots` (campo 4) e `ask1_lots` (campo 19) dello schema `BOOK_5` sono **certificate** — schema `[BID×5 best-first][ASK×5 best-first]`, triplo `(lots, orders, price)`, confermato su 29 eventi / 290 triple FIB6F `[rif. reviews/REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md, M-10]`; ordine BID-poi-ASK confermato e indice del triplo risolto da `lots >= orders` (290/290). La cautela \"verifica parziale → Empirico-CLI\" di Cap.47 e' **saldata**.)*"

### File:riga toccati

| File | Riga | Modifica |
|------|------|----------|
| `docs/methodology_v2/CAP_09_parte_9.md` | 173 | annotazione (A) tabella `bar_synthetic` — parentesi RM-1 da "verifica parziale" a "certificato W3/M-10" |
| `docs/methodology_v2/CAP_09_parte_9.md` | 177 | annotazione (B) bullet prosa — idem |
| `reports/REPORT_CAP_09.md` | — | questa sezione |

Nessun'altra riga di `CAP_09_parte_9.md` toccata. In particolare **NON toccati**: la mappatura schema CANDLE (Cap.49:155-171), la regola `bar_synthetic` stessa (formula mid, gate `bid1_lots≥1 AND ask1_lots≥1`, casi b/c), Cap.47:93 (home dello schema `BOOK_5`, fuori scope del task), la tabella codici errore Cap.50, l'indice, gli AC.

### Note di stato

- **Parte 9 resta PASS storico `86425a7`**; questa modifica e' **solo annotazione di stato** (Verifica parziale → Certificato), **nessun cambiamento alla mappatura/metodologia**, alla regola di derivazione `bar_synthetic`, alle posizioni-campo dichiarate, ne' al comportamento runtime. I campi citati (campo 4/6/19/21) coincidono con quelli gia' impliciti nello schema `BOOK_5` di Cap.47:93 — la patch non introduce numeri nuovi, ridenomina lo *stato di certezza*.
- **Soggetto a probe-review RM-4(b) prima del commit**: la patch modifica un fatto gia' dichiarato in un CAP PASS (RM-4 criterio (b) — "modifica un fatto gia' dichiarato 'verificato' in CAP precedenti") → probe-review obbligatoria. **Nessun commit eseguito**: gli edit restano nel working tree; `tasks/DEV_STATUS.md = READY_FOR_PROBE_REVIEW docs/methodology_v2/CAP_09_parte_9.md`. Il commit e' compito dell'Orchestratore dopo PASS della probe-review.

### Criterio di rollback (micro-patch)

Reversibile per pura sostituzione testuale: se la probe-review smentisse la certificazione (es. trovasse che l'audit CLI ha certificato solo l'ordine dei blocchi ma non l'indice del triplo, o solo un sottoinsieme delle posizioni), si ripristina l'annotazione "verifica parziale → Empirico-CLI" originale, o la si riscrive al livello di certezza effettivamente supportato. Il rollback non tocca codice ne' metodologia (l'annotazione e' descrittiva). Trigger: qualunque finding BUG REALE della probe-review sulla fedelta' della citazione rispetto a `REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md` / M-10.

---

## Micro-patch post-PASS #2 (2026-06-02) — Cap.47:93 schema BOOK_5 → CERTIFICATE [W3/M-10]

**Natura**: secondo micro-patch chirurgico di **sola annotazione di stato** su Parte 9 (PASS storico `86425a7`), NON una nuova iterazione del ciclo Planner→Developer→Reviewer. Aggiorna la caveat RM-1 della **home dello schema `BOOK_5`** (Cap.47, riga 93) da "Verifica parziale (RM-1) … Empirico-CLI" a "Verificato `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`".

### Motivazione

La micro-patch #1 (sezione precedente) ha aggiornato a CERTIFICATE le sole **annotazioni di Cap.49** (riga 173 tabella `bar_synthetic` + riga 177 bullet prosa), entrambe ora chiuse con la frase "la cautela 'verifica parziale → Empirico-CLI' di Cap.47 e' **saldata**". Ma la **home dello schema `BOOK_5`** (Cap.47:93) — la caveat che Cap.49 dichiara saldata — era rimasta ancora in stato "Verifica parziale (RM-1) … Empirico-CLI". Restava quindi una **incoerenza interna**: Cap.49 rimandava a una cautela di Cap.47 dichiarata saldata mentre Cap.47 la presentava ancora come aperta. Questa patch #2 **chiude OM-2 della probe-review della patch #1** (coerenza Cap.47↔Cap.49), allineando la caveat sorgente di Cap.47 allo stato di certezza gia' riflesso in Cap.49. Nota chiave: la caveat originale chiedeva esplicitamente "cattura di ≥N eventi `BOOK_5` su FIB front-month liquido (Empirico-CLI)" — ed e' **esattamente** cio' che l'audit CLI W3 ha fatto (29 eventi su FIB6F front-month).

### Fonte citata (autoritativa, NON ri-derivata)

- `reviews/REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md` §W3 (righe 55-73): `BOOK_5` = `[BID×5 best-first][ASK×5 best-first]`, ogni livello triplo `(lots, orders, price)`; **certificato bit-a-bit su 29 eventi / 290 triple FIB6F front-month liquido**. Ordine BID-poi-ASK escluso invertito (blocco 1 sempre discendente = BID, 29/29 eventi, riga 62); triplo `(lots, orders, price)` confermato da `lots ≥ orders` su 290/290 (esclude `(orders, lots, price)`, riga 65); `bid1_price < ask1_price` su 29/29 (riga 64); anomalia 27/05 `bid1>ask1` **NON riprodotta** = artefatto del campione illiquido FIB6I a scadenza lontana, non inversione di schema (righe 68, 72).
- `tasks/STATO_CORRENTE.md` §5 **M-10** (riga 77): stessa certificazione; "`f1≥f2` su 290/290 (lots≥orders) esclude triplo invertito; `bid1<ask1` su 29/29; anomalia 27/05 `bid1>ask1` = artefatto del campione (FIB6I illiquido scadenza lontana), NON inversione schema".

**RM-1 onesto**: la nuova caveat certifica SOLO cio' che W3 certifica davvero — ordine blocchi, indice del triplo, `bid1<ask1`, non-riproduzione dell'anomalia. Mantiene esplicitamente la nota che **il decoder canonico D non parsa `BOOK_5`**: la certificazione e' empirica **level-1** (probe CLI diretto), NON level-2 (codice di produzione). Non sovra-estende oltre il campione (non dichiara nulla sui livelli 2-5 oltre l'ordinamento dei prezzi gia' osservato).

### Prima → dopo (testuale)

**Cap.47, bullet `BOOK_5`, riga 93 (`docs/methodology_v2/CAP_09_parte_9.md:93`)** — sostituita SOLO la caveat finale del bullet; la definizione dello schema `BOOK_5;<TICKER>;...` e l'esempio del probe `BOOK_5;FIB6I;14:02:33;...` restano identici.

- PRIMA: "*__Verifica parziale (RM-1): lo schema BOOK_5 deriva da una singola osservazione del 2026-05-27__ (un solo evento FIB6I) e non è corroborato dal decoder canonico (D non parsa BOOK_5). Alternative compatibili __non escluse__: ordine dei blocchi BID/ASK (potrebbe essere invertito — nel campione `bid1_price=49715.0` risulta maggiore di `ask1_price=49275.0`, anomalo per un book regolare, plausibilmente perché contratto a scadenza lontana ma non disambiguato), indice del triplo `lots`/`orders`/`price` per livello. La struttura va disambiguata con cattura di ≥N eventi `BOOK_5` su FIB front-month liquido (Empirico-CLI).*"
- DOPO: "*__Verificato (RM-1) `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`:__ lo schema `BOOK_5` e' __certificato__ dalla cattura di 29 eventi / 290 triple su FIB6F front-month liquido `[rif. reviews/REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md, M-10]` — esattamente la disambiguazione che questa caveat richiedeva (cattura di ≥N eventi `BOOK_5` su FIB front-month liquido). Risolte le alternative prima non escluse: __ordine dei blocchi BID-poi-ASK confermato__ (blocco 1 sempre discendente = BID su 29/29 eventi, NON invertito), __indice del triplo `(lots, orders, price)` confermato__ da `lots >= orders` su 290/290 triple (esclusa l'alternativa `(orders, lots, price)`), e `bid1_price < ask1_price` su 29/29 eventi. L'__anomalia `bid1_price=49715.0 > ask1_price=49275.0` del campione singolo 27/05__ (che lasciava aperta l'ipotesi di blocchi invertiti) e' risultata __NON riprodotta__ sul front-month liquido: era un artefatto del contratto illiquido FIB6I a scadenza lontana (book rado/crossato su contratto poco scambiato), non un'inversione di schema. Il decoder canonico D continua a non parsare `BOOK_5`: la certificazione e' empirica diretta a livello-1 via probe CLI `[PROVA-EMPIRICA 2026-06-01]`, non level-2 da codice di produzione.*"

### File:riga toccati

| File | Riga | Modifica |
|------|------|----------|
| `docs/methodology_v2/CAP_09_parte_9.md` | 93 | caveat finale del bullet `BOOK_5` — da "Verifica parziale (RM-1) … Empirico-CLI" a "Verificato `[PROVA-EMPIRICA 2026-06-01 W3 / M-10]`" |
| `reports/REPORT_CAP_09.md` | — | questa sezione |

Nessun'altra riga di `CAP_09_parte_9.md` toccata. In particolare **NON toccati**: la **definizione dello schema** `BOOK_5;<TICKER>;...` (resta identica) e l'**esempio del probe** `BOOK_5;FIB6I;14:02:33;...` sul medesimo rigo 93; il bullet `ANAG` (r.92) e il bullet `PRICE` (r.94); le annotazioni Cap.49 r.173/177 (gia' patchate dalla micro-patch #1); la tabella codici errore Cap.50; l'indice; gli AC.

### Note di stato

- **Parte 9 resta PASS storico `86425a7`**; modifica = **solo annotazione di stato della caveat** (Verifica parziale → Certificato), **schema ed esempio del probe invariati**, nessun cambiamento alla mappatura/metodologia, alla regola di derivazione `bar_synthetic`, alle posizioni-campo dichiarate, ne' al comportamento runtime.
- Con questa patch #2, Cap.47:93 (home dello schema) e Cap.49:173/177 (annotazioni che vi rimandano) sono **coerenti**: la cautela dichiarata "saldata" in Cap.49 e' ora effettivamente saldata anche nella sua sorgente di Cap.47.
- **Soggetto a probe-review RM-4(b) prima del commit**: la patch modifica un fatto gia' dichiarato in un CAP PASS (RM-4 criterio (b) — "modifica un fatto gia' dichiarato 'verificato' in CAP precedenti") → probe-review obbligatoria. **Nessun commit eseguito**: gli edit restano nel working tree; `tasks/DEV_STATUS.md = READY_FOR_PROBE_REVIEW docs/methodology_v2/CAP_09_parte_9.md`. Il commit e' compito dell'Orchestratore dopo PASS della probe-review.

### Criterio di rollback (micro-patch #2)

Reversibile per pura sostituzione testuale: se la probe-review smentisse la certificazione o la fedelta' della citazione rispetto a `REVIEW_CAP_DATA_02_RM_RETRO_CLI_review.md` §W3 / M-10, si ripristina la caveat "Verifica parziale (RM-1) … Empirico-CLI" originale (riportata integralmente sopra in "PRIMA"), o la si riscrive al livello di certezza effettivamente supportato. Il rollback non tocca codice ne' metodologia (la caveat e' descrittiva). Trigger: qualunque finding BUG REALE della probe-review sulla fedelta' della citazione o sull'eventuale sovra-dichiarazione (es. certificazione che in realta' copre solo un sottoinsieme delle alternative escluse).
